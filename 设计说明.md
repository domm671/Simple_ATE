# simple_ATE 设计说明文档

> Simple to understand. Simple to configure. Simple to extend. Simple to deploy.

- 文档版本：v0.1（草案）
- 适用软件版本：simple_ATE v0.1
- 状态：评审通过（2026-09-09，设计决策见第 14 章）

---

## 1. 概述

### 1.1 软件定位

simple_ATE 是一款运行在 ATE/FT 测试工位上位机上的测试执行软件。它按预先编写的测试脚本，通过 CAN / RS485 / TCP 等通信方式与下位机测试装备交互，自动完成测试流程、判定结果、记录数据并上传 MES。

### 1.2 设计原则

1. **刻意保持小**：只做 UI、脚本引擎、通信、日志、存储、MES 接入六件事。
2. **脚本即协议，写脚本不改代码**：CAN 帧的 ID、数据、应答匹配、字节解析全部以标签声明在脚本中，引擎负责拼帧、发送、等待与解析；仅多帧握手/校验和等复杂场景才使用可选的 Python 扩展点。
3. **先无头后界面**：引擎不依赖 UI，可命令行独立运行，便于无硬件开发与自动化测试。
4. **真实设备与模拟设备等价**：通信资源可在真实硬件与 Mock 应答脚本间切换，无硬件即可开发、调试、演示。
5. **结果绝不丢失**：测试结果先本地落盘，再异步上传 MES，网络异常不影响生产。
6. **渐进演进**：v0.1 只做顺序流程，不做 DSL 编译器、表达式引擎、插件系统。

### 1.3 v0.1 范围

| 分类 | 内容 |
|---|---|
| **v0.1 包含** | XML 脚本解析、顺序执行、内建通信原语（拼帧发送/应答等待/字段解析）、上下限判定、timeout、retry、delay、人工停止/中止、可选自定义 action 扩展点、运行日志、JSON 文本结果存储（预留数据库接口）、Mock 设备、命令行运行、基础 PySide6 界面、MES 上传扩展点（Outbox 机制，协议由定制方实现） |
| **v0.2 再做** | `if` 条件分支、`loop` 循环、sub-script 子脚本、变量运算、serial 帧原语、位级字段提取、CAN FD |
| **暂不做** | 插件系统、ORM、用户权限、Web Server、分布式、微服务、消息队列、AI、多设备并发 |

---

## 2. 术语

| 术语 | 含义 |
|---|---|
| Script（脚本） | XML 格式的产品测试流程定义 |
| Step（测试步） | 脚本中一个最小的测试/动作单元 |
| Frame（帧） | 一条 CAN 报文：ID、扩展帧标志、数据字节 |
| Field（字段） | 从应答帧中按字节偏移提取并换算的测量值，自动成为脚本变量 |
| Action（扩展动作） | 可选的 Python 扩展调用，仅用于多帧/校验和等内建原语无法覆盖的场景 |
| Communication（通信） | 物理链路层，只负责帧的收发 |
| Resource（资源） | 工位上的一条逻辑通信链路，如 `can_main` |
| Context（上下文） | 一次测试运行期间的变量表（field 产出）、SN、资源句柄等 |
| Run | 一次完整测试（扫描一个 SN，执行一遍脚本） |

---

## 3. 总体架构

```text
┌──────────────────────────────────────────────┐
│                    UI (PySide6)              │
│  脚本选择 │ SN │ Start/Stop │ 进度 │ 结果 │ 日志 │
└──────────────────────┬───────────────────────┘
                       │ Qt Signal
┌──────────────────────┴───────────────────────┐
│              Engine Event Adapter             │
└──────────────────────┬───────────────────────┘
                       │ 事件回调（引擎不依赖 Qt）
┌──────────────────────┴───────────────────────┐
│                 Script Engine                 │
│      Parser → Executor → Judge → Context      │
└───────┬───────────────┬───────────────┬──────┘
        │               │               │
        ▼               ▼               ▼
┌────────────────┐ ┌──────────────┐ ┌──────────────┐
│ Frame IO 内建   │ │ ResultStore  │ │ MES Adapter  │
│ send/wait/field│ │ JSON / DB IF │ │ + Outbox     │
└──────┬─────────┘ └──────────────┘ └──────────────┘
       │   （可选）extensions/ 中的自定义 action 同样只经此口收发
       ▼
┌──────────────┐
│Communication │  CAN（v0.1）/ Serial（预留）/ Mock（同步接口）
└──────┬───────┘
       ▼
┌──────────────┐
│ Test Equipment│
└──────────────┘
```

横切关系：

- **Logger** 被所有层使用，输出运行日志与原始通信 trace。
- **MES 接入不进入脚本**，由引擎在 Run 结束后通过 MES 扩展点上传；本项目不定义、不内置任何具体 MES 接口协议。

典型执行流：

```text
UI Start / CLI
   ▼
加载脚本 → XML 解析为 Step 列表
   ▼
逐 Step：拼帧发送 → 等待应答 → 按字段解析为变量 → 判定 → 发事件 → 写存储
   ▼
Run 结束 → 生成 TestResult → 本地落盘
   ▼
MES 上传（经定制方实现的 Adapter，失败进 Outbox 重试）
```

---

## 4. 模块设计

### 4.1 脚本引擎（engine）

引擎是软件核心，**不依赖任何 UI 框架**，由四个部分组成：

| 子模块 | 职责 |
|---|---|
| `parser.py` | XML → Step 对象模型；加载时做结构校验，失败立即报错，不进入执行 |
| `executor.py` | 按顺序执行 Step；内建原语拼帧收发与字段解析；处理 timeout、retry、delay、停止标志、异常分类；发出事件回调 |
| `context.py` | 维护一次 Run 的 SN、变量表（field 产出）、资源句柄、结果累积 |
| `extension.py` | 可选自定义 action 的加载（importlib，仅限工位配置放行的扩展目录） |

**v0.1 绝大多数测试步不需要任何 Python 代码**：脚本通过内建的 `<send>/<wait>/<field>` 原语直接描述帧收发与解析（详见第 5 章与《ATE脚本格式规范.md》）。脚本中不允许出现任意 Python 代码；仅当协议涉及多帧握手、校验和、UDS 或特殊串口协议时，才通过 `<action handler="模块:函数"/>` 调用 `extensions/` 目录中的定制函数，模块必须在工位配置 `[extensions]` 中显式放行，引用错误在加载期即可发现。

#### 引擎事件回调

执行器通过一个事件回调接口对外通知，UI 或 CLI 各自实现适配：

```python
class EngineListener:
    def on_run_start(self, context): ...
    def on_step_start(self, step): ...
    def on_step_finish(self, step, item_result): ...
    def on_log(self, level, message): ...
    def on_run_finish(self, run_result): ...
```

- CLI 模式：回调直接打印。
- UI 模式：Engine 运行在工作线程，Adapter 中用 Qt 信号（`queued connection`）转发到主线程。

### 4.2 通信原语与可选扩展（无独立驱动层）

v0.1 **不设独立产品驱动层**。CAN 帧的拼装、发送、应答等待与字段解析都是引擎内建能力，直接由脚本标签声明：

```text
<send id=... data=.../> 拼帧发出
        ↓
<wait id=... timeout> 按 ID（+mask）匹配应答，丢弃无关帧，收到前先清空接收队列
        ↓
<field offset=0 length=2 endian=little gain=0.01 var="vbat"/>
        从应答帧取原始整数 → raw*gain+bias → 工程值变量
        ↓
<limit value="${vbat}" min=... max=.../> 判定
```

设计取舍：

- **协议细节暴露在脚本中是有意为之**：编写装备脚本的人同时掌握被测协议，帧细节写在脚本里即改即测，不需要为此修改、重新发布项目代码；
- 工位级链路参数（通道、接口型号、波特率）仍然只属于工位配置，不进脚本；
- CAN ID、字节布局、缩放系数等与具体自定义 CAN 协议相关的信息由脚本承载，项目不引入 DBC/cantools；
- v0.1 原语面向**经典 CAN（11/29 位 ID，0–8 字节）**的一发一收模型；serial/TCP 报文、CAN FD、多帧分包、位级提取放到后续版本或用扩展 action 解决。

**可选扩展点（例外而非常态）**：当一个测试步需要多帧握手、校验和、UDS、状态机等待等原语无法描述的逻辑时，在 `extensions/` 目录放一个普通 Python 模块，脚本以 `<action handler="my_uds:enter_extended_session" var="..."/>` 调用；扩展函数从上下文获得通信资源句柄自行收发，签名约定见《ATE脚本格式规范.md》。自定义 action 与内建原语适用同一套 timeout/retry/on_fail 语义。

### 4.3 通信层（communication）

**v0.1 采用同步接口，不使用 asyncio。** 底层 `python-can`、`pyserial` 均为同步库，FT 测试为严格顺序流程，引入 async 只会增加复杂度。

```python
class Communication(Protocol):
    def open(self) -> None: ...
    def close(self) -> None: ...
    def send(self, frame: Frame) -> None: ...
    def recv(self, timeout: float) -> Frame: ...   # timeout 单位秒，超时抛 TimeoutError
```

`Frame` 为统一帧对象（方向、ID/地址、数据字节、时间戳、扩展帧标志等）。

实现类：

- `can.py`：基于 python-can；
- `serial.py`：v0.1 仅保留抽象接口占位（内建帧原语暂不支持串口报文）；
- `mock.py`：按资源配置的**应答脚本文件**应答（请求 ID+数据 → 应答帧），供无硬件开发与单元测试。

所有原始收发帧写入 trace 日志（见 4.4）。

### 4.4 日志（logging）

两类日志严格分开：

**(1) 运行日志 run log**（文本文件 + 界面日志区，按天分文件）

```text
2026-09-09 12:00:01 INFO  [RUN] BMS20260909001 test started, script=BMS_FT v1.0
2026-09-09 12:00:02 INFO  [CAN] channel can_main opened @500kbps
2026-09-09 12:00:03 INFO  [STEP] Voltage start
2026-09-09 12:00:03 INFO  [STEP] Voltage = 12.03 V  -> PASS
2026-09-09 12:00:04 ERROR [STEP] CAN_COMM timeout after 2 retries -> ERROR
```

**(2) 原始通信 trace**（每次 Run 单独文件，如 `trace_<SN>_<时间戳>.log`）

```text
[12:00:03.112] TX can_main id=0x18FF50E5 ext=1 data=02 01 00 00 00 00 00 00
[12:00:03.145] RX can_main id=0x18FF50E5 ext=1 data=02 01 0B C6 ...
```

trace 用于协议排查与质量追溯，不进入 UI，按配置保留天数自动清理。

### 4.5 存储（storage）

v0.1 **不内置数据库**，测试结果以 **JSON 文本文件**保存：UTF-8、人可读、可直接作为 MES 上传 payload，日后也可批量导入任意数据库。运行日志与通信 trace 不进结果存储。

存储层面向 `ResultStore` 抽象接口编程，内置唯一实现为文件存储 `FileResultStore`；**数据库（SQLite / SQL Server 等）实现仅作为扩展点预留，由使用本项目做定制化开发的人员自行实现**，在工位配置中切换即可，引擎与 MES 层代码无需改动。

为防止崩溃丢数据，采用**每步完成即落盘**（覆写该 Run 的 JSON 文件并 flush），Run 结束写入最终结论。文件布局、JSON 格式、Outbox 与扩展接口详见第 9 章。

### 4.6 MES 接入（mes）—— 仅提供扩展点

**本项目不定义、不内置任何具体 MES 接口协议**（不规定 HTTP REST、报文格式、鉴权等）。项目只提供两样东西：

1. **标准化的测试结果数据**：即第 9 章定义的 `RunResult` / JSON 文件，这是项目可向 MES 提供的全部信息；
2. **上传扩展点与 Outbox 机制**：上传时机、离线缓存、退避重试由项目负责，具体协议由使用本项目的定制方实现。

```python
class MesUploader(Protocol):
    def upload(self, result: RunResult) -> None:
        """由定制方实现：按其 MES 接口协议上传 result。
        成功返回；失败抛异常，由 Outbox 负责重试。
        本项目提供 NullUploader（不上传，仅本地归档）作为默认实现。"""
```

**离线缓存（Outbox）机制**（基于文件目录，无需数据库）：

1. Run 结束 → JSON 结果文件写入 `results/pending/`；
2. Uploader 线程/定时任务扫描 pending 目录，调用配置中指定的定制 `MesUploader`；成功后移动文件到 `results/uploaded/`；
3. 上传失败时文件保留在 pending，同名 `.retry` sidecar 文件记录失败原因、次数与下次重试时间，按退避策略重试（如 10s / 1min / 5min / 30min）；超过最大重试次数移入 `results/failed/` 等待人工处理；
4. `mes.enabled = false`（或使用默认 `NullUploader`）时结果文件直接归档到 `uploaded/`，Uploader 不扫描；
5. 提供 UI 状态指示（待传数量）与手动重传按钮；
6. **MES 故障永远不阻塞测试、不改变测试结论。**

### 4.7 UI（PySide6）

v0.1 单窗口，五个区域：脚本选择、SN 输入、Start/Stop/Reset、测试进度、结果与日志。

要求：

- 扫描枪即键盘输入，SN 框回车即启动（可配置）；
- SN 默认不做格式校验（任意非空 SN 均可启动）；使用方可在配置中启用 `sn.validation_enabled` 并设置 `sn.pattern` 正则，启用后非法 SN 禁止启动；
- 测试中禁止再次 Start，Stop 可随时点击；
- PASS 绿色 / FAIL 红色 / ERROR 黄色 / ABORT 灰色，结果字号醒目；
- UI 只做展示与操作，所有业务逻辑在引擎；
- 布局预留后续"操作员模式 / 工程师模式"切换。

---

## 5. 脚本格式规范（XML v1）

> **完整语法、属性类型、变量引用、参数绑定、判定/重试/超时语义、错误码与 XSD 见独立文档《[ATE脚本格式规范.md](./ATE脚本格式规范.md)》（Script XML v1，权威定义）。** 本章仅给出总体规则与示例。

### 5.1 总体规则

- 根节点 `<test>`，编码 UTF-8；
- 节点即语句类型，属性即参数；
- 仅允许本规范定义的标签，未知标签解析即失败；
- 变量引用语法 `${name}`，仅做简单取值，不支持表达式；
- 脚本文件与产品相关，**不得包含任何工位硬件参数**（通道号、波特率等放工位配置）；
- **测试项参数（上下限、延时、重试次数等）直接配置在脚本中**，不随产品工单切换；不同产品/不同规格使用不同脚本文件（如 `bms_ft_100ah.xml`、`bms_ft_200ah.xml`），由 UI 选择；
- 运行时记录脚本文件的 SHA-256 校验和与版本号，用于追溯。

### 5.2 完整示例

下例使用扩展帧（29 位 ID）自定义 CAN 协议：请求与应答 ID 不同，测量值为小端整数经缩放换算。

```xml
<?xml version="1.0" encoding="UTF-8"?>
<test name="BMS_FT" version="1.0">

    <connect resource="can_main" timeout="3"/>

    <!-- 通信自检：发 ping，等待应答，只关心应答是否到达 -->
    <step name="CAN_COMM" timeout="2" retry="2" retry_interval="0.5" on_fail="abort">
        <send id="0x18FF50E5" ext="true" data="01 00"/>
        <wait id="0x18FF50E6" ext="true"/>
    </step>

    <step name="Voltage" timeout="5" on_fail="continue">
        <send id="0x18FF50E5" ext="true" data="02 01 00"/>
        <wait id="0x18FF50E6" ext="true">
            <field offset="0" length="2" endian="little" gain="0.01" var="vbat"/>
        </wait>
        <limit value="${vbat}" min="11.5" max="12.5" unit="V"/>
    </step>

    <step name="SleepCurrent" timeout="5" on_fail="continue">
        <send id="0x18FF50E5" ext="true" data="03 02"/>
        <wait id="0x18FF50E6" ext="true">
            <field offset="0" length="4" endian="little" gain="0.1" var="i_sleep"/>
        </wait>
        <limit value="${i_sleep}" max="1000" unit="uA"/>
    </step>

    <step name="RelayCheck" timeout="3" retry="1" on_fail="abort">
        <send id="0x18FF50E5" ext="true" data="10 01"/>
        <delay ms="200"/>
        <send id="0x18FF50E5" ext="true" data="11 01"/>
        <wait id="0x18FF50E6" ext="true">
            <field offset="0" length="1" var="relay_st"/>
        </wait>
        <limit value="${relay_st}" eq="1"/>
    </step>

    <disconnect resource="can_main"/>

</test>
```

> 完整语法（`id_mask`、`min_len`、`bias`、裸 `send` 控制帧、独立 `<wait>` 只监听等）以《[ATE脚本格式规范.md](./ATE脚本格式规范.md)》为准。

### 5.3 节点定义（v0.1）

| 节点 | 位置 | 关键属性 | 说明 |
|---|---|---|---|
| `test` | 根 | `name`, `version` | 脚本名与版本 |
| `connect` | 顶层 | `resource`, `timeout`(s) | 打开工位配置中定义的逻辑通信资源 |
| `disconnect` | 顶层 | `resource` | 关闭资源；异常/中止时由引擎保证执行 |
| `step` | 顶层 | `name`必填, `resource`(单资源可省), `timeout`, `retry`, `retry_interval`, `on_fail` | 一个测试步，仅在一个资源上收发 |
| `send` | step 内 | `id`, `data`, `ext`, `id_mask` | 拼帧发送；`data` 为空格分隔的十六进制字节，可内嵌单字节 `${var}` |
| `wait` | step 内 | `id`, `ext`, `id_mask`, `timeout`, `drain` | 等待匹配应答帧；可含多个 `field`；空体只做“应答到达”检查 |
| `field` | wait 内 | `var`必填, `offset`, `length`, `endian`, `gain`, `bias`, `raw`, `unit` | 从应答帧提取字段并换算为变量 |
| `action` | step 内 | `handler`必填, `var`可选，其余为入参 | **可选**：调用 `extensions/` 中经配置放行的 Python 函数 |
| `limit` | step 内 | `value`, `min`, `max`, `eq`, `unit` | 判定，至少给一种判据；min/max 为闭区间 |
| `delay` | 内/顶层 | `ms` | 可被停止标志中断的延时 |

执行规则：

1. step 内指令严格按顺序执行：`send` / `wait`（提取 field 变量）/ `delay` / `action`，最后 `limit`；
2. step 至少含一条通信指令（`send`/`wait`/`action`），至多一个 `limit` 且必须在最后；
3. `wait` 在收发前默认清空接收缓冲（`drain="before"`），按 ID（可选 mask）匹配，无关帧丢弃；
4. field 换算：`工程值 = 原始整数 × gain + bias`（默认 gain=1、bias=0）；`raw="true"` 时不做换算；
5. **retry 仅对通信超时/异常生效；判定 FAIL 不重试**；
6. 通信异常重试耗尽 → ERROR；判定超限 → FAIL；`on_fail` 决定终止或继续；
7. 引擎在每条指令边界及 delay 期间检查停止标志。

### 5.4 v0.2 预留标签（v0.1 解析到即报错）

```xml
<if var="vbat" gt="10"> ... </if>
<loop count="3"> ... </loop>
<call script="BMS_COMMON" />
```

---

## 6. 结果模型与状态机

### 6.1 四种结果状态

| 状态 | 含义 | 处置 |
|---|---|---|
| **PASS** | 所有必测项合格 | 放行，上传 MES |
| **FAIL** | 产品测量值不合格 | 判产品不良，记录不良项，上传 MES |
| **ERROR** | 设备/工装/通信异常，非产品问题 | **不得计为产品不良**，提示检查工装，可立即复测 |
| **ABORT** | 人工停止或脚本安全中止 | 不下产品结论，记录中止位置 |

Run 结论汇总规则（走完所有语句、未中止时）：

- 任一步为 **ERROR** → Run = **ERROR**（ERROR 优先于 FAIL：工装异常时不对产品下结论）；
- 否则任一步为 **FAIL** → Run = **FAIL**；
- 全部 PASS → Run = PASS；
- 因 step `on_fail=abort` 在 FAIL 处终止 → Run = FAIL；在 ERROR 处终止 → Run = ERROR；
- 人工 Stop → Run = ABORT（中止前已产生的明细保留，但总状态为 ABORT）。

### 6.2 单步结果数据

```python
@dataclass
class ItemResult:
    seq: int
    step_name: str
    value: float | str | None
    unit: str | None
    low_limit: float | None
    high_limit: float | None
    result: str          # PASS / FAIL / ERROR
    duration_ms: int
    retries: int
    error_code: str | None
    message: str | None
```

---

## 7. 工位配置（TOML）

脚本与工位硬件解耦：脚本引用逻辑资源名，实际通道在工位配置中定义。配置文件使用 **TOML**（Python ≥ 3.11 标准库 `tomllib` 原生读取，不引入 YAML）。

`config/station.toml` 示例：

```toml
station_id = "FT_LINE1_ST03"
software_version = "0.1.0"

# SN 校验（扫描枪输入）
[sn]
validation_enabled = false  # 默认不校验；需要时置 true 并按产品规则填写 pattern
# pattern = "^BMS[0-9]{11}$"   # validation_enabled=true 时生效
auto_start = true           # 扫码回车即启动

[storage]
sink = "file"               # file（内置 JSON 文本存储）| custom（定制方实现数据库等）
result_dir = "./data/results"
log_dir = "./data/logs"
trace_retain_days = 30

# 逻辑资源 can_main：真实 CAN 与 mock 二选一
[resources.can_main]
type = "can"               # can（v0.1 内建帧原语）| serial（仅预留，需扩展 action）| mock
# type="can" 时生效
interface = "pcan"
channel = "PCAN_USBBUS1"
bitrate = 500000
# type="mock" 时生效：按应答脚本文件回复（JSON，请求 id+data → 应答 id/data）
mock_script = "./config/mock/bms_can.json"

# 可选自定义 action 的放行清单（模块相对 extensions/ 目录）
[extensions]
allowed = []               # 如 ["my_uds"] → 脚本中 handler="my_uds:enter_session"

[mes]
enabled = false                     # true 时需由定制方提供 uploader 实现
# uploader 为定制方实现的类，形如 "myplant.mes:HttpMesUploader"
# uploader = "custom.mes_client:MyMesUploader"
retry_intervals = [10, 60, 300, 1800]   # 秒，退避重试
max_retry = 8
```

---

## 8. 线程模型

```text
UI 主线程 (Qt event loop)
   │ Start
   ▼
WorkerThread ── 运行 Engine（全部同步调用）
   │                 │
   │                 ├── Frame IO → Communication（阻塞 recv，受 wait/step timeout 约束）
   │                 └── 每步检查 threading.Event 停止标志
   │
   └── 通过 Qt Signal（queued）回主线程刷新界面

MES UploaderThread ── 独立线程/定时轮询 Outbox，与测试线程互不影响
```

要点：

- UI 线程**绝不**允许直接调用通信层；
- 停止通过 `threading.Event` 通知，不强制 kill 线程；
- 所有资源（CAN/串口）在引擎 `finally` 中统一 `disconnect`；需要下电、断继电器等安全动作时，在脚本末尾安排裸 `send` 控制帧（或用自定义 action 注册清理回调），异常与 ABORT 时同样执行；
- 结果文件每次写入后立即 flush；Uploader 仅在上传成功后移动文件，测试线程与上传线程不会同时操作同一文件。

---

## 9. 结果存储设计

### 9.1 目录布局

```text
data/results/
├── pending/      # 待上传 MES（每个 Run 一个 JSON）
│   ├── BMS20260909001_20260909_120001.json
│   └── BMS20260909001_20260909_120001.retry   # 仅上传失败时存在（sidecar）
├── uploaded/     # 上传成功（或 MES 禁用时直接归档于此）
└── failed/       # 超过最大重试次数，等待人工处理
```

文件名规则：`<SN>_<yyyyMMdd_HHmmss>.json`。**同一 SN 允许多条 Run 记录，时间戳保证文件名唯一，任何情况下不得覆盖历史记录**（同一秒内重复启动时追加序号后缀）。软件启动时若发现 pending 中存在缺少 `result` 字段的残留文件（上位机异常退出），将其结果标记为 `ABORT`。

### 9.2 结果文件格式（JSON）

```json
{
    "sn": "BMS20260909001",
    "script_name": "BMS_FT",
    "script_version": "1.0",
    "script_sha256": "9f2c…",
    "software_version": "0.1.0",
    "station_id": "FT_LINE1_ST03",
    "start_time": "2026-09-09T12:00:01",
    "end_time": "2026-09-09T12:00:35",
    "result": "PASS",
    "items": [
        {
            "seq": 2,
            "step_name": "Voltage",
            "value": 12.03,
            "unit": "V",
            "low_limit": 11.5,
            "high_limit": 12.5,
            "result": "PASS",
            "duration_ms": 142,
            "retries": 0,
            "error_code": null,
            "message": null
        }
    ]
}
```

字段与第 6 章数据模型一一对应。文件随测试执行逐步覆写：`run_start` 时创建（仅头部字段），每步完成追加 item 后整体落盘，`run_finish` 写入 `end_time` 与最终 `result`。

### 9.3 ResultStore 预留接口

引擎、Uploader 只依赖该抽象接口，不关心后端是文件还是数据库：

```python
class ResultStore(Protocol):
    def create_run(self, meta: RunMeta) -> RunHandle: ...
    def append_item(self, handle: RunHandle, item: ItemResult) -> None: ...
    def finish_run(self, handle: RunHandle, result: str) -> RunResult: ...

    # 供 MES Outbox 使用
    def pending(self) -> list[RunResult]: ...
    def mark_uploaded(self, run: RunResult) -> None: ...
    def mark_retry(self, run: RunResult, reason: str,
                   next_retry_at: datetime) -> None: ...
    def mark_failed(self, run: RunResult, reason: str) -> None: ...
```

- **`FileResultStore`**：v0.1 内置实现，即 9.1/9.2 的 JSON 文本方案；
- **定制实现**（如 `SqliteResultStore`、`SqlServerResultStore`、`MesDirectStore`）：由使用本项目的定制化开发人员实现该接口，在启动装配处注册，并将 `station.toml` 中 `sink` 改为 `custom`。引擎、UI、MES 模块均无需修改。

### 9.4 数据库接入指引（给定制方）

参考表结构（test_run / test_item 两表，字段与 9.2 JSON 完全同名）、索引（sn、mes_status）和事务边界建议见项目 `docs/customization.md`（M1 后补充）；v0.1 代码中**不包含**任何数据库驱动与 ORM 依赖。

---

## 10. 目录结构

```text
simple_ate/
├── pyproject.toml
├── main.py                         # 入口：ui / run 子命令
├── config/
│   ├── station.toml                # 工位配置（不入库、随工位部署）
│   └── mock/                       # Mock 资源的应答脚本
│       └── bms_can.json
├── scripts/                        # XML 产品测试脚本（帧细节直接写在里面）
│   └── bms_ft.xml
├── extensions/                     # 可选自定义 action（仅放复杂协议代码；普通产品无需）
│   └── README.md
├── data/                           # 运行时生成（gitignore）
│   ├── results/                    # pending/ uploaded/ failed/
│   └── logs/
├── src/simple_ate/
│   ├── cli.py                      # 无头运行：python -m simple_ate run bms_ft.xml --sn xxx
│   ├── engine/
│   │   ├── parser.py               # XML → Step（含 send/wait/field/action 绑定检查）
│   │   ├── executor.py             # 顺序执行 / 拼帧收发 / 字段提取 / timeout / retry / 停止
│   │   ├── frame_io.py             # 内建 send/wait/field 的拼帧、匹配与解析
│   │   ├── context.py
│   │   ├── extension.py            # 自定义 action 的 importlib 加载与白名单
│   │   └── model.py                # Step / Send / Wait / Field / ItemResult / RunResult
│   ├── communication/
│   │   ├── base.py                 # 同步抽象接口 + Frame
│   │   ├── can.py
│   │   ├── serial.py               # v0.1 仅接口占位
│   │   └── mock.py                 # 按 mock 应答脚本回复
│   ├── storage/
│   │   ├── base.py                 # ResultStore 抽象接口（数据库等实现预留）
│   │   └── file_store.py           # 内置 JSON 文本存储 + Outbox 目录
│   ├── mes/
│   │   ├── base.py                 # MesUploader 接口 + NullUploader 默认实现
│   │   └── outbox.py               # 离线缓存与退避重试（扫描 pending 目录）
│   ├── logging_conf/
│   │   └── logger.py
│   ├── config_loader.py
│   └── ui/
│       ├── main_window.py
│       └── engine_bridge.py        # 引擎事件 → Qt Signal
└── tests/
    ├── test_parser.py
    ├── test_executor.py
    └── test_judge.py
```

---

## 11. 技术栈与依赖

| 用途 | 选型 | 备注 |
|---|---|---|
| 语言 | Python ≥ 3.11 | 使用 tomllib |
| UI | PySide6 | |
| CAN | python-can | |
| 串口 | pyserial | 有 RS485 需求时再装 |
| MES 协议 | **不内置** | 仅定义 `MesUploader` 接口与标准结果数据，HTTP/数据库/报文等实现由定制方按需引入，v0.1 不强制安装 requests/httpx |
| XML 解析 | 标准库 `xml.etree.ElementTree` | 仅加载内部可信脚本；如未来支持外部载入则换 `defusedxml` |
| 结果存储 | 内置 JSON 文本文件 | 数据库仅预留 `ResultStore` 接口，由定制方实现，v0.1 不引入任何 DB 依赖 |
| 打包 | PyInstaller | 单目录/单文件部署到工位机 |

依赖最小化，除上表外不引入框架。

---

## 12. 里程碑

### M1 — 无头引擎 + Mock（地基，不碰 UI/真硬件） ✅ 已完成

- XML 解析与校验、顺序执行、内建 send/wait/field 原语、自定义 action 加载；
- limit 判定、timeout、retry、delay、停止标志；
- Mock 通信（应答脚本 JSON）；
- 结果 JSON 文本落盘（每步即时写盘）、run log 与 trace；
- CLI：`python -m simple_ate run scripts/bms_ft.xml --sn BMS20260909001`；
- parser / executor / 判定 / 扩展 / 存储共 57 个单元测试全部通过（`python -m unittest discover -s tests`）。

**退出标准（已达成）**：无硬件、无界面条件下，命令行完整跑出示例脚本并生成正确的 PASS/FAIL/ERROR/ABORT 记录。进程退出码：PASS=0、FAIL=1、ERROR=2、ABORT=3。

### M2 — PySide6 界面 ✅ 已完成

- 五区域主窗口（脚本选择 / SN 扫码 / Start-Stop-Reset / 逐项进度表 / 结果大字+日志）；
- SN 默认不做 pattern 校验（任意非空 SN 均可启动）；使用方按需置 `sn.validation_enabled=true` 配置正则，启用后非法 SN 禁止启动；扫码回车启动受 `sn.auto_start` 控制；
- 工作线程（QThread + moveToThread）运行引擎，`EngineBridge` 以 Qt 信号（自动 queued connection）回主线程刷新；UI 线程不接触通信层；
- 测试中禁用 Start/脚本/SN，Stop 可随时点击（走引擎停止标志，安全收尾），Reset 清空；
- PASS 绿 / FAIL 红 / ERROR 黄 / ABORT 灰；结束自动清空 SN 并聚焦，便于连续扫码；
- MES 启用时显示“待传 MES：N”计数（上传与退避重试在 M4）；
- 无脚本/解析错误等提示采用非阻塞状态栏，避免卡住产线操作；
- 脚本编辑器（`ui/script_editor.py`）：脚本栏“新建脚本…”/“编辑脚本…”打开，支持两种编辑方式：① XML 原文编辑；② 图形化编辑（树形节点 + 属性表单，可添加/删除/上下移 connect/step/send/wait/field/limit/action/delay）；两页签互相同步，保存前复用 `ScriptParser` 做全量静态校验（错误码/行列/说明一次列出），校验不过不写文件；经图形页签往返不保留 XML 注释；
- 入口 `simple-ate-gui` 或 `python -m simple_ate gui`；PySide6 为可选依赖 `[gui]`；
- 新增脚本编辑器 16 个测试（offscreen 平台覆盖双向同步/增删移节点/action 扩展入参/校验拦截/新建与原地保存），累计 83 个测试全部通过。

**退出标准（已达成）**：在 Mock 环境下用界面完成选脚本 → 扫码 → 自动执行 → 实时进度/颜色 → 结论展示 → 复位复测的完整闭环，且停止可在数百毫秒内响应。

### M3 — 真实 CAN 联调

- python-can 适配（11/29 位 ID、收发与超时）；
- 工位配置切换真实/Mock；
- 用实际装备协议验证脚本原语表达能力，固化 timeout 与重试参数；原语覆盖不到的场景沉淀到 `extensions/` 示例。

### M4 — MES 扩展点与 Outbox

- `MesUploader` 扩展接口、`NullUploader` 默认实现、定制类的配置化加载；
- Outbox 退避重试、手动重传（结果本身即 JSON 文件，无需额外导出功能）；
- 具体 MES 协议实现不在本项目内，由现场定制开发提供。

---

## 13. 非目标（明确不做）

- 通用 DSL / 脚本编译器 / 表达式引擎；
- 脚本中执行任意 Python 代码；
- 插件系统、多产品动态加载框架；
- 用户权限、审计、Web 服务、远程控制；
- 内置数据库存储实现与 ORM（已预留 `ResultStore` 接口，交由定制方实现）、内置具体 MES 协议（已预留 `MesUploader` 接口）、消息队列、微服务；
- 多设备并行测试（单总线顺序执行覆盖全部 v0.1 场景）。

---

## 14. 设计决策记录（已确认）

1. **BMS 协议载体：自定义 CAN 协议，且帧细节直接声明在测试脚本中**。引擎内建拼帧、ID 匹配应答与按字节偏移解析（含 gain/bias 换算），不使用 DBC/cantools；普通产品测试**无需编写任何项目代码**，多帧/校验和/UDS 等复杂场景才通过 `extensions/` 自定义 action 扩展；
2. **MES 接口协议：本项目不做定义**。项目仅提供标准测试结果数据（`RunResult` / JSON）与 `MesUploader` 扩展点及 Outbox 机制；具体接口协议（HTTP/数据库/私有报文、鉴权、字段映射）由使用本项目的定制化开发人员实现；
3. **SN 复测策略：同一 SN 允许多条 Run 记录，永不覆盖**。结果文件名以时间戳区分，复测历史完整保留；
4. **测试项参数：不随产品工单切换，直接在测试脚本中配置**（上下限、timeout、retry、延时等）；不同产品规格使用不同脚本文件，由 UI 选择。v0.2 之前不引入工单/参数集机制。
